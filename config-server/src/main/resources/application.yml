server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DongJu-Na/spring-cloud-sample-config-repo
          search-paths: settings/**
          default-label: master